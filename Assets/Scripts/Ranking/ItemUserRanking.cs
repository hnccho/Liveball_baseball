using UnityEngine;
using System.Collections;

public class ItemUserRanking : MonoBehaviour {

	public EntryListInfo mInfo;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	public void LoadImage(){
//		UtilMgr.LoadImage(mInfo.imageName
//		transform.FindChild("SprPhotoBG").FindChild("Photo").FindChild("Texture").GetComponent<UITexture>());
	}
}
